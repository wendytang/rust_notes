use std::fmt::Debug;

fn main() {
    println!("See examples directory for monomorphization demonstrations!");
    println!("Run them using:");
    println!("cargo run --example basic");
    println!("cargo run --example multiple");
}